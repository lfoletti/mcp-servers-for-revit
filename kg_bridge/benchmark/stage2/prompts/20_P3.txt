P3 — create-for-real, then structural query.

You are working on a Revit building model. Do the work for REAL in the
open Revit model (create the actual elements), then answer from the
project state.

1. Create level "N0" at elevation 0 mm.
2. Create exactly 4 walls on level N0, 4000 mm long, 2700 mm high,
   end to end along X (wall k from x=4000*(k-1) to x=4000*k, k=1..4),
   all of the same basic wall type.
3. Create exactly 4 windows, each hosted on a DISTINCT wall (one
   window per wall), sill height 900 mm, head height 2100 mm.

Then answer this question, working from the project state (not from
this conversation): which windows are on level N0, and which wall
hosts each? Give the host wall identifier (e.g. ElementId) for every
window. Report counts too.
