Refactor — level rename with reference preservation.

Execute this task NOW with the tools available. Do NOT ask clarifying
questions — `AskUserQuestion` is not available in this mode. Do NOT
defer execution or wait for confirmation.

Step 1 — Seed. Create:
- Level R_N0 at elevation 0
- Level R_N1 at elevation 3000

Then create 6 walls (3 per level), all basic wall type, height 2700:
- R_WALL_N0_a on R_N0, (0,0)→(4000,0)
- R_WALL_N0_b on R_N0, (4000,0)→(8000,0)
- R_WALL_N0_c on R_N0, (8000,0)→(12000,0)
- R_WALL_N1_a on R_N1, (0,0)→(4000,0)
- R_WALL_N1_b on R_N1, (4000,0)→(8000,0)
- R_WALL_N1_c on R_N1, (8000,0)→(12000,0)

Step 2 — Rename the two levels:
  R_N0  -> L00_GROUND
  R_N1  -> L01_FIRST

The 6 walls MUST stay on their original respective level (no wall
should jump from R_N0 to R_N1 or vice versa). The renames must
preserve the host relationship.

Step 3 — Verify. After the rename, query your tools and report:
  - The 2 new level names and their elevations.
  - For each of the 6 walls, its current host level name.
End with a one-line confirmation that no wall changed level (i.e.
R_WALL_N0_* are on L00_GROUND, R_WALL_N1_* are on L01_FIRST).
