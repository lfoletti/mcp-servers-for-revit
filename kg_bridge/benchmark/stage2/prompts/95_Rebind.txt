Rebind — delete + recreate identical element.

Execute this task NOW with the tools available. Do NOT ask clarifying
questions — `AskUserQuestion` is not available in this mode. Do NOT
defer execution or wait for confirmation.

Step 1 — Seed. Create level RB_N0 at elevation 0, then create 4 walls
on RB_N0, basic wall type, height 2700, end-to-end:
- RB_WALL_1 (0,0)→(4000,0)
- RB_WALL_2 (4000,0)→(8000,0)
- RB_WALL_3 (8000,0)→(12000,0)
- RB_WALL_4 (12000,0)→(16000,0)

Step 2 — Delete RB_WALL_2 entirely (and update any project-memory
state to reflect that deletion).

Step 3 — Create a NEW wall identical to RB_WALL_2 in geometry, type,
and host level, but with a fresh identifier RB_WALL_2_NEW.

Step 4 — Report. Query your tools and report:
  - How many walls currently exist on RB_N0?
  - What is the identifier of the wall that occupies x=4000→8000 now?
  - Is the old RB_WALL_2 still referenced anywhere (in any state)? If
    so, where, and as what (active / deleted / orphaned)?
End with a one-line summary of the final wall count.
