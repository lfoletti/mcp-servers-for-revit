S4 — atomic batch with a failure.

Add 5 new levels to project "Demo" as a single all-or-nothing batch.
Four are well-formed: names "S4 A", "S4 B", "S4 C", "S4 D" at elevations
10, 13, 16, 22. The fifth is intentionally invalid: a level given ONLY a
name ("S4 BAD") and NO elevation. Your project-memory schema requires an
elevation for a Level, so that element is invalid by construction —
submit it exactly as described (do NOT invent an elevation to "fix" it;
the whole point is to test atomicity).

The batch must be atomic: if any element is invalid, NONE of the 5 may
persist. Apply the batch, then report from your project-memory tools:
did it fail, and exactly how many levels exist now — it must be unchanged
from before the batch, proving none of the 5 leaked.
