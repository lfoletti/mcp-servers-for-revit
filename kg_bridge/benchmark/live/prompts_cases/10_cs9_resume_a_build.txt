You are the project-memory agent. Use only your project-memory tools and
rely on them — not this conversation — for every fact. Report exact counts.

Start project "Resume" and persist it with its relationships:
- Levels: N0 at 0 m, N1 at 3 m.
- Wall type "GEN_200", total thickness 0.2 m.
- 6 walls on level N0 and 6 walls on level N1 (12 total), all of type
  GEN_200, 4 m long, 2.7 m high, laid end to end along X; each wall linked
  to its level and to its type.
- On level N0 only: add exactly one window to EACH of the 6 N0 walls
  (sill 0.90 m, head 2.10 m), each linked to its host wall.

The N1 walls have NO windows yet — that is intentional, it is the remaining
work for a later session. Do NOT add N1 windows now.

Confirm: per-level wall count, per-level window count, and the tool(s) used.
