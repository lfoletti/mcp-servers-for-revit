You are tracking a Revit project across sessions using the project-memory
tools available to you.

Create project "RoomBench20": one level N0 at elevation 0 m, and exactly
20 rooms on level N0, named R-0001 through R-0020, each with area 20.0 m².

Persist every room — do not stop until all 20 are persisted. IMPORTANT: if a
bulk/batch creation tool is available, create all 20 rooms in a SINGLE call;
only if no batch tool exists, create them one per call.

Then confirm, from the project-memory tools, the exact number of rooms
persisted in "RoomBench20" and which tool(s) you used.
