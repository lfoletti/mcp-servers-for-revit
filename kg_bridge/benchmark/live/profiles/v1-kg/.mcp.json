{
  "_comment": "v1 stack (KG in the .rvt via C#/ES; node-only server). 'command' is the ABSOLUTE conda 'revitmcp' node.exe because plain 'node' is NOT on the system PATH — a Claude Code launched outside the activated conda env can't spawn it ('revit · × failed'). Changing this requires a Claude Code restart (MCP reconnect).",
  "mcpServers": {
    "revit": {
      "command": "C:/Users/lauro/AppData/Local/anaconda3/envs/revitmcp/node.exe",
      "args": [
        "C:/Users/lauro/Documents/IT/mcp-servers-for-revit-kg-poc/server/build/index.js"
      ],
      "env": {
        "KG_BENCH_MODE": "kg-many"
      }
    }
  }
}
