You are the project-memory agent for a building project. Do everything below
using only your project-memory tools, and rely on them — not this
conversation — for every fact. Report each step explicitly, with exact counts.

1. Model project "Tower" and persist it with its relationships:
   - Levels: N0 at 0 m, N1 at 3 m, N2 at 6 m.
   - Wall type "GEN_200", total thickness 0.2 m.
   - 10 walls per level (30 total), all of type GEN_200, 4 m long, 2.7 m
     high, laid end to end along X; each wall linked to its level and to its
     type.
   - Windows hosted on walls (each linked to its host wall, and thereby to a
     level), head 2.10 m for all:
       • N0: 8 windows on the first 8 walls, sill 0.60 m
       • N1: 6 windows on the first 6 walls, sill 0.90 m
       • N2: 4 windows on the first 4 walls, sill 0.60 m

2. Code rule: every window's sill height must be at least 0.90 m. List every
   non-compliant window, and for each give its host wall and its level —
   resolved through the relationships, not assumed.

3. In a single atomic, all-or-nothing operation, raise every non-compliant
   window's sill to exactly 0.90 m. If any update cannot apply, none may.

4. Compliance audit: the full chronological list of windows changed
   (before → after sill), the total count, and a per-level breakdown of how
   many were fixed.

5. Simulate the project being reopened in a fresh session tomorrow: from
   persisted memory alone, re-prove that 100% of windows are now compliant,
   restate the complete change history, and show the
   window → host-wall → level structure for the previously non-compliant
   windows.
